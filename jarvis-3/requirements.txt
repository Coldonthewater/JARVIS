# --- Module 1: Core Backend ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# --- Module 2: AI Brain ---
httpx>=0.27,<1.0
openai>=1.54,<2.0
google-genai>=0.3,<1.0
anthropic>=0.39,<1.0

# --- Testing ---
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0

# Future modules will add their dependencies here as they're built
# so this file always reflects exactly what the current state of the
# project needs.
