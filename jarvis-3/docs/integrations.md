# Jarvis — Integrations

_Not yet applicable — the `Integration` plugin interface is built in Module 5._

Once built, this document will define:
- The shared `Integration` interface every service must implement
- Per-integration notes: auth method (OAuth scopes, API key, etc.), rate limits, quirks
- Status of each planned integration (Microsoft 365, Google, Spotify, Home Assistant, Hue, etc.)
