# Jarvis — API Reference

_Not yet applicable — the gateway (REST + WebSocket API) is built in Module 3._

Once built, this document will primarily point to the auto-generated
OpenAPI schema (FastAPI provides this for free at `/docs` and `/openapi.json`),
plus document the WebSocket event contracts that aren't covered by OpenAPI.
