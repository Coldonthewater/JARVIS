# Jarvis — Client Contracts

_Not yet applicable — the client registry and capability negotiation schema are built in Module 3 (Gateway)._

Once built, this document will define exactly what a new client (mobile, wall
display, AR glasses, etc.) needs to implement to connect to the gateway:
authentication, capability declaration, and the event/data contracts it can expect.
